git add .
git config --global user.email "mdd94@cornell.edu"
git config --global user.name "Michelle Davies"
git config --global credential.helper store
git commit -m "hardware conn updates"
git push --all
